# S3 Backend Configuration
# Inject this during initialization using: terraform init -backend-config=s3.tfbackend

bucket         = "nonprod-terraform-state"
key            = "eem-manager/test/terraform.tfstate"
region         = "us-east-1"
encrypt        = true
dynamodb_table = "nonprod-terraform-state-locks"